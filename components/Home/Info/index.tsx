import styles from './index.module.scss'

const HomeInfo = () => (
	<section className={styles.root}>
		<p className={styles.subtitle}>
			Share your code with one command. Allow others to run your code in an
			online IDE.
		</p>
	</section>
)

export default HomeInfo
